# Exclude from WordPress.org SVN archive.
# Run bin/build-release.sh before deploying — it cleans vendor/ down to
# the autoloader only. This file only needs to exclude source/dev artefacts.
/.git
/.github
/.ddev
/bin
/docs
/tests
composer.json
composer.lock
phpunit.xml.dist
phpcs.xml
.distignore
.gitignore
/vendor-prefixed/league/html-to-markdown/bin
